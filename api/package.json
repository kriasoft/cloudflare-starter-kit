{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "yarn api:build",
    "deploy": "yarn api:deploy"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.2.0",
    "core": "workspace:*",
    "web-auth-library": "^0.3.1"
  },
  "devDependencies": {
    "@jest/globals": "^28.1.0"
  }
}
